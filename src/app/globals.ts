export const endpoint = (HOST != null) ? `${HOST}/api` : '/api'

export const encryptionChunkSize = 131072

export const uploadChunkSize = 4194304
// export const uploadChunkSize = 150994944 // max in firefox